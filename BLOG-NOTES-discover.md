# 블로그용 정리 — "/discover" 섹션 추가 (2026-08-18)

이 문서는 이번에 추가한 `/discover` 섹션 관련 작업 내용을 블로그 글로 옮기기 쉽게 정리해둔 메모다. 실제 코드/커밋 내용을 그대로 요약한 것이라, 글을 쓸 때 이 문서에서 필요한 부분만 골라 살을 붙이면 된다.

---

## 1. 무엇을 왜 만들었나

**Before**: 여기다(yeogida-life.com)는 정부·공공기관이 운영하는 서비스만 다루는 큐레이션 디렉토리였다. `data/services.json`에 등록된 127개 서비스 전부가 보건복지부, 경찰청, 국토교통부 같은 공공기관 소관이었다.

**계기**: "공공서비스뿐 아니라 다른 여러 사이트도 소개하고 싶다"는 아이디어에서 출발. 다만 기존 사이트의 핵심 신뢰 포지셔닝("링크모음이 아니라 공익적 큐레이션")을 지키기 위해, 민간/일반 사이트를 기존 데이터에 섞지 않고 **완전히 분리된 섹션**으로 만들기로 결정했다.

**결과**: `/discover`라는 새 URL 아래, 기존과는 다른 색상/타이포/카드 디자인을 가진 독립 섹션을 신설. 카테고리 체계, 검색, 상세 페이지, 관리자 화면까지 기존 서비스 디렉토리와 동일한 수준의 기능을 갖췄지만 콘텐츠와 디자인은 완전히 별개다.

---

## 2. 의사결정 포인트 (글감으로 좋은 부분)

- **"필터로 통합" vs "완전히 별도 섹션"**: 처음엔 기존 사이트에 `sourceType: public/private` 필드만 추가해서 뱃지/필터로 구분하는 쪽을 제안했다(브랜드 자산 하나로 유지, 유지보수 부담 적음). 하지만 사용자가 "완전히 다른 디자인으로 별도 섹션"을 명확히 선택했다 — 신뢰 포지셔닝이 다른 두 콘텐츠를 한 브랜드에 억지로 묶기보다, 처음부터 분리하는 편이 맞다는 판단.
- **상호 링크는 지금 안 건다**: 두 섹션을 완전히 독립 운영하기로 함. 나중에 붙일 수도 있지만 지금은 의도적으로 끊어둠.
- **콘텐츠보다 구조 먼저**: `data/sites.json`, `data/site-categories.json`을 빈 배열로 시작해서, "일단 틀을 만들고 콘텐츠는 관리자 화면에서 직접 채운다"는 방식을 택함. 이 결정 덕분에 이번 작업 범위가 명확해졌고(스키마/라우팅/디자인 인프라), 실제 사이트 리서치·검수는 별도 트랙으로 미룰 수 있었다.

---

## 3. 기술적으로 눈에 띄는 부분

### (1) Next.js "Route Groups"로 완전히 다른 사이트를 한 저장소에서 운영
`app/(main)/`과 `app/(discover)/`라는 두 개의 route group으로 나눴다. 괄호로 감싼 폴더명은 URL에 나타나지 않는 Next.js App Router 관례라, 기존 URL(`/`, `/category/...`, `/service/...`)은 전혀 바뀌지 않으면서 두 섹션이 완전히 다른 `<Header>`/`<Footer>`/메타데이터/색상 테마를 가질 수 있었다.

루트 `app/layout.tsx`는 두 섹션이 공통으로 필요한 것(폰트, Google Tag Manager, Google Analytics, 애드센스 스크립트)만 담당하는 "얇은 shell"로 남기고, 실제 헤더/푸터/JSON-LD는 각 route group의 `layout.tsx`가 따로 정의하는 구조로 리팩터링했다.

> 블로그 포인트: "한 코드베이스, 한 배포 파이프라인 안에서 완전히 다른 톤앤매너의 사이트 두 개를 운영하는 법"이라는 주제로 풀어볼 수 있음. Route Groups는 이럴 때 쓰라고 있는 기능.

### (2) CSS 변수 재정의만으로 컴포넌트 라이브러리 통째로 재스킨
`/discover` 섹션은 shadcn/ui 기반 컴포넌트(Card, Badge, Button 등)를 코드 수정 없이 그대로 재사용한다. 비결은 Tailwind v4의 `@theme inline` 패턴 — `--color-primary`, `--radius` 같은 시맨틱 토큰이 실제 값을 참조하는 간접 변수로 정의되어 있어서, `[data-theme='discover']` 셀렉터 안에서 그 변수들만 다시 정의하면 같은 컴포넌트가 완전히 다른 배색/모서리 반경으로 렌더링된다. (메인: 파란 계열 + 둥근 카드, discover: 보라 계열 + 각진 카드)

> 블로그 포인트: "디자인 시스템을 토큰화해두면 브랜드를 몇 줄의 CSS 변수 재정의로 통째로 바꿀 수 있다"는 실전 사례.

### (3) 겪은 버그: Next.js `title.template` 상속 규칙
`(discover)/layout.tsx`에 `title: { default: '여기다 발견', template: '%s | 여기다 발견' }`를 정의했는데, `/discover` 홈 페이지의 `<title>`이 "여기다 발견 | 여기다"로 이중 접미사가 붙는 버그가 있었다. 원인은 Next.js 공식 문서에 명시된 규칙: **레이아웃의 `title.default`는 자기 자신의 template이 아니라 "가장 가까운 부모 세그먼트"의 template에 의해 다시 감싸진다.** 즉 discover 레이아웃의 default 값이 루트 레이아웃의 template("%s | 여기다")에 의해 한 번 더 감싸진 것. 해결은 `discover/page.tsx`에 `title: { absolute: '여기다 발견' }`을 명시해서 상위 template 적용을 완전히 차단하는 것.

> 블로그 포인트: "Next.js metadata title 상속, 직관과 다르게 동작하는 부분" — 실제로 겪은 디버깅 스토리라 개발 블로그 소재로 좋음.

### (4) Windows 개발 환경에서 겪은 파일 잠금 이슈
route group으로 폴더 구조를 재편하려고 `git mv app/admin "app/(main)/admin"`를 실행했는데 `Permission denied`로 실패. 원인은 `npm run dev`(Next.js/Turbopack)가 해당 폴더의 파일을 잠그고 있었기 때문. dev 서버 프로세스를 찾아 종료한 뒤에야 이동이 가능했다.

> 블로그 포인트: 사소하지만 "대규모 폴더 재구성 전엔 dev 서버부터 끄자"는 실용적인 팁.

---

## 4. 새로 생긴 것들 (파일/구조 목록)

- **라우트**: `/discover`(홈), `/discover/[categorySlug]`(카테고리별 목록), `/discover/site/[slug]`(상세), `/admin/sites`(관리자 — 카테고리 추가, 사이트 등록/수정/삭제, 썸네일·이미지 업로드)
- **데이터**: `data/sites.json`, `data/site-categories.json` (아직 빈 배열 — 관리자 화면에서 채워야 함)
- **디자인**: `app/globals.css`에 `[data-theme='discover']` 테마 블록 추가, `components/discover/` 아래 전용 Header/Footer/SiteCard/CategoryNav/SearchBar/SiteDetail
- 기존 서비스 관련 코드는 전부 `app/(main)/` 아래로 이동했을 뿐 동작은 그대로 (URL도 그대로)

## 5. 아직 안 한 것 / 다음에 다룰 거리

- `/discover`에 실제 사이트 콘텐츠가 하나도 없음 — 다음 글감은 "카테고리/사이트를 처음 등록하는 과정" 자체가 될 수 있음.
- 애드센스 스크립트가 최상위 shell에 있어서 `/discover`에도 자동으로 노출되는 상태 — 콘텐츠가 쌓인 뒤 "일반 링크모음 = 신뢰도 리스크" 관점에서 재검토 예정.
- 기존 사이트와 `/discover` 사이에 상호 링크는 아직 없음 — 의도적인 선택.
