---
omd: 0.1
brand: "여기다"
bootstrapped_from: karrot
bootstrapped_at: "2026-08-04"
---

# Design System Inspiration of 여기다 (yeogida-life.com)

## 1. Visual Theme & Atmosphere

여기다는 "몰라서 못 쓰는, 진짜 도움되는 생활 서비스"를 찾아주는 큐레이션 디렉토리다. 디자인은 콘텐츠(서비스 카드) 자체를 주인공으로 두고, 차분한 블루 포인트와 뉴트럴, 직설적인 언어, 가벼운 인터페이스 크롬으로 그 신뢰를 뒷받침한다. Primary는 `#2563eb` 하나로 통일해 쓴다 — 제품·마케팅을 구분하는 별도 색을 두지 않는다.

**핵심 특징:**
- Semantic Primary: `#2563eb` (단일 브랜드 컬러, 랜딩 CTA도 동일하게 사용)
- 시스템 폰트 우선(`Pretendard Variable`을 폴백으로 선언), 브랜드 전용 서체 없음
- 콘텐츠 밀도가 높은 카드형 목록 — 급하게 정보를 찾는 사용자가 스캔하기 쉬운 리듬
- 절제된 장식, 신뢰는 색이 아니라 명확한 정보 구조에서 나온다는 전제

## 2. Color Palette & Roles

### 제품 시맨틱
- **Primary** (`#2563eb`): 주요 CTA(바로가기, 검색 버튼), 활성 상태. 랜딩/마케팅 화면에서도 동일하게 사용 — 별도 색을 두지 않는다.
- **Primary Hover / Pressed** (`#5182ef`): 라이트 테마 상태 매핑 (Primary 80% 오퍼시티 기준).
- **Canvas** (`#ffffff`): 기본 배경.
- **Background** (`#f2f3f6`): 페이지 배경(카드 바깥 영역).
- **Surface** (`#f7f8fa`): 카드/패널 표면.
- **Foreground** (`#212124`): 본문 텍스트.
- **Muted** (`#868b94`): 보조 텍스트(운영시간, 출처 등 메타데이터).
- **Hairline** (`#eaebee`): 구분선.
- **Brand Tint** (`#e9f0fe`): 강조 배경(뱃지, 하이라이트 카드).
- **Danger** (`#fa2314`): 오류, 링크 만료 표시.
- **Info** (`#009ceb`): 안내 배지("24시간", "신규" 등과 구분되는 정보성 배지).
- **Success** (`#1aa174`): "무료" 배지, 제보 접수 완료 등.

## 3. Typography Rules

### 폰트 패밀리
- **웹 UI**: 시스템 폰트 우선(`-apple-system` 계열), `Pretendard Variable`을 폴백으로 선언.
- **모노스페이스**: 별도 지정 없음 — 새로 만들지 말 것.

### 위계

| 역할 | 폰트 | 크기 | 굵기 | 줄 높이 | 자간 | 비고 |
|------|------|------|--------|-------------|----------------|-------|
| H1 | System | 48px | 700 | 135% | 0 | 카테고리 페이지 제목 |
| H2 | System | 42px | 700 | 135% | 0 | 섹션 타이틀 |
| H3 | System | 34px | 700 | 135% | 0 | 서브섹션 |
| Title | System | 24px | 700 | 135% | 0 | 카드 그룹 제목 |
| Body L1 | System | 16px | 400 | 150% | -0.02em | 서비스 설명 본문 |
| Body L2 | System | 14px | 400 | 150% | -0.02em | 메타데이터, 캡션 |

### 원칙
- 굵기는 regular/bold 두 단계만 사용 — 커스텀 웨이트 스케일을 만들지 않는다.
- 헤딩은 자간 중립, 본문은 좁은 자간(-0.02em)을 유지한다.

## 4. Component Stylings

### 랜딩 웹

**Primary CTA**
- Background: `#2563eb`
- Text: `#ffffff`
- Radius: 9999px
- Padding: 4px 12px
- Height: 36px
- Font: 14px / 500 / System
- 용도: 헤더/히어로의 핵심 액션(검색하기, 서비스 제보하기)

### 제품 컴포넌트 (Karrot 원본 계승)

**Box Button**
- 상태: primary, primary-low, secondary, danger, disabled, hover, keyboard
- 용도: 카드 내 바로가기 버튼, 필터 토글

**Text Field**
- 상태: outlined, underlined, focused, disabled, readonly, required, invalid
- 용도: 검색창, 서비스 제보 폼 입력

**Tabs**
- 상태: selected, disabled, focus
- 용도: 카테고리 전환("무료만", "24시간만" 등 필터 포함)

**Snackbar**
- 상태: default, success, warning, action focus
- 용도: 제보 접수 완료, 바로가기 클릭 확인 등 짧은 피드백

## 5. Layout Principles

### 스페이싱 시스템
- 기본 단위: 4px
- 관찰된 클러스터: 4px, 8px, 12px, 16px, 64px
- 더 큰 여백은 섹션 구성에만 쓰고 전역 토큰으로 확장하지 않는다.

### 그리드 & 컨테이너
- 카테고리/검색 목록은 반응형 카드 그리드, 중앙 정렬된 콘텐츠 컬럼.
- 모바일 엣지 인셋 16px 유지(실사용자 대부분이 모바일 검색 유입).

### 여백 철학
- 콘텐츠(서비스 카드)가 주인공, 크롬은 조용하게.
- 4/8/12/16 리듬을 로컬하게 사용하고, 문서화되지 않은 값은 만들지 않는다.

### 라운드 스케일
- Small: 6px
- Medium: 8px
- Full: 9999px (버튼, 뱃지)

## 6. Depth & Elevation

기본은 flat(`box-shadow: none`)이다. 카드 구분은 그림자가 아니라 `#eaebee` hairline과 `#f7f8fa` surface 대비로 처리한다.

## 7. Do's and Don'ts

### Do
- Primary `#2563eb`는 제품 인터랙션(버튼, 활성 상태)에만 사용.
- 시스템 폰트를 기본으로 하고, 필요할 때만 Pretendard Variable로 보강.
- 각 컴포넌트의 documented states/접근성 동작을 그대로 따른다.

### Don't
- 문서화되지 않은 새 브랜드 컬러를 만들지 않는다.
- 마케팅 스냅샷에서 카드/시트 geometry를 임의로 추정하지 않는다.
- 신뢰를 뱃지·자물쇠 아이콘으로 과장하지 않는다 (§12 참고).

## 8. Responsive Behavior

- 헤더 CTA는 컴팩트 36px pill, 히어로 CTA는 40–48px.
- Box Button은 5개 사이즈, 모바일 16px 엣지 인셋 필수.
- Tabs는 카테고리 5개 이하면 fill, 6개 이상/가변이면 hug.
- Text Field 라벨/설명은 줄바꿈 가능, 입력값 자체는 한 줄로 유지하고 넘치면 clip.

## 9. Agent Prompt Guide

### 컬러 퀵 레퍼런스
- Product Primary: `#2563eb`
- Product Hover / Pressed: `#5182ef`
- Canvas: `#ffffff`; Background: `#f2f3f6`; Surface: `#f7f8fa`
- Foreground: `#212124`; Muted: `#868b94`; Hairline: `#eaebee`
- Danger: `#fa2314`; Info: `#009ceb`; Success: `#1aa174`

### 예시 프롬프트
- "Primary `#2563eb`로 서비스 상세 카드의 바로가기 버튼을 만들어줘 — Box Button의 documented states를 그대로 따라서."
- "랜딩 히어로 CTA를 `#2563eb` 배경, 흰 텍스트, 14px/500 System, 36px 높이, full-pill radius로 만들어줘."
- "검색창을 outlined/underlined variant, 명확한 label, invalid/focused/disabled 상태까지 포함해서 만들어줘."

### 반복 가이드
1. 시스템 타이포그래피를 기본으로 시작한다.
2. 4/8/12/16/64 스페이싱만 정식 값으로 취급한다.
3. 컴포넌트 상태는 §4 기준을 따른다.

---

## 10. Voice & Tone

여기다는 "급하게 검색했는데 안 나오던 정보를 미리 찾아둔 이웃"처럼 말한다: 따뜻하고, 쉽게 풀어 쓰고, 진입장벽 없이, 광고 카피처럼 들리는 표현은 쓰지 않는다. 사용자가 아이 응급상담을, 혹은 밤늦게 문 연 약국을 찾고 있다는 전제를 항상 지키며, 반말이 아닌 부드러운 존댓말(`-어요`, `-예요`)을 쓰되 딱딱한 `-습니다`체는 최소화한다.

| 상황 | 톤 |
|---|---|
| CTA | 짧고 동사 중심 (`바로가기`, `제보하기`, `자세히 보기`) |
| 빈 상태 | 왜 비어있는지 한 줄 설명 + 낮은 압박의 제안. `데이터가 없습니다` 금지. |
| 오류 메시지 | 구체적이고 책임 소재를 묻지 않으며 실행 가능하게. `다시 시도해 주세요`를 선호. |
| 완료 토스트 | 과거형 한 문장 (`제보가 접수되었어요`). 조용하게, 과장 없이. |
| 서비스 설명 | 무엇을 해주고 언제 써야 하는지 먼저, 자격 조건은 그 다음. |
| 신뢰 신호 | 침착하고 사실 기반으로. 운영 주체(정부기관/민간)를 명시하되 겁주지 않는다. |
| 긴급상황 카테고리 | 특히 침착하고 명확하게 — 과장된 긴급 문구로 불안을 부추기지 않는다. |
| 온보딩 | 한 화면, 한 개념, 한 행동. 불릿 나열이나 기능 투어 금지. |

**금지 표현.** `불편을 드려 죄송합니다`, `죄송하지만`, `데이터가 없습니다`, `오류가 발생했습니다`, `혁신적인`, `놀라운 혜택`, `업계 최고`. 이모지는 커뮤니티성 콘텐츠(제보 감사 인사 등)에 아주 가끔만 허용하고, 오류 메시지·긴급상황 안내·신뢰 관련 문구에는 절대 쓰지 않는다.

## 11. Brand Narrative

여기다(yeogida-life.com)는 "검색해도 잘 안 나오는, 진짜 도움되는 생활 서비스를 한 곳에 모아서 알려준다"는 문제의식에서 시작했다. 24시간 소아 상담, 무료 법률구조공단 상담, 청소년상담 1388, 심야약국 찾기처럼 존재는 하지만 아는 사람만 아는 서비스들을 카테고리별로 정리해, 급할 때 검색창 앞에서 막막해지는 순간을 줄이는 것이 목표다.

여기다가 거부하는 것: 성인·도박 사이트 우회 접속용으로 변질된 기존 "링크모음" 사이트의 이미지, 그리고 단순히 링크만 나열하는 얕은 정보성. 대신 각 서비스가 무엇을 해주고, 누가, 언제 이용하면 되는지까지 설명하는 콘텐츠로 신뢰를 쌓는다. 운영 주체(정부기관/민간)를 항상 함께 표기하는 것도 같은 이유다 — 신뢰는 뱃지가 아니라 출처의 투명성에서 나온다.

## 12. Principles

1. **Primary는 아껴 쓴다.** `#2563eb`는 CTA와 활성 상태에만 쓰고, 배경이나 장식에는 쓰지 않는다. *적용:* 한 화면에 Primary CTA가 두 개 이상 경쟁하면 하나는 뉴트럴로 낮춘다.
2. **시스템 폰트, 콘텐츠가 브랜드다.** 커스텀 서체를 만들지 않는다. 사용자가 찾는 건 서비스 정보지 브랜드 타이포그래피가 아니다. *적용:* 강조가 필요하면 웨이트 700을 쓰지, 디스플레이 서체를 도입하지 않는다.
3. **출처는 항상 드러낸다.** 모든 서비스 카드는 운영 주체(정부기관/민간)와 무료/유료 여부를 숨기지 않는다. *적용:* source·cost 필드는 상세 화면 안이 아니라 카드 목록에서부터 바로 보이게 한다.
4. **신뢰는 침착함에서 온다.** 자물쇠 아이콘이나 "검증됨" 트로피 배지로 신뢰를 과장하지 않는다. 일관성과 정확한 정보로만 신뢰를 얻는다. *적용:* 경고성 문구는 실제 오류에만 red를 쓰고, 평상시 안내는 뉴트럴 톤을 유지한다.
5. **모든 값은 4px 그리드 위에 있다.** `{4, 8, 12, 16, 20, 24, 32, 40, 48, 56, 64}` 밖의 값은 근거를 남기거나 수정한다.
6. **하나의 액센트, 하나의 시스템.** 세컨더리 브랜드 컬러를 새로 만들지 않는다. success/danger/info는 시맨틱 컬러이지 브랜드 컬러가 아니다.
7. **콘텐츠 밀도는 높게, 크롬은 가볍게.** 사용자는 한 세션에 여러 서비스를 훑어본다. 모바일 뷰포트에서 카드 3–4개는 보여야 하고, 장식 요소가 이를 방해해서는 안 된다.

## 13. Personas

이 프로젝트가 실제로 상정하는 이용자 맥락(가상 데모그래픽이 아니라 PRD 기반 실제 타겟):

- **육아맘/대디:** 아이가 갑자기 아플 때, 야간·응급 상담 서비스를 빠르게 찾아야 한다. 침착하고 명확한 안내가 최우선.
- **사회초년생·1인가구:** 법률·행정·소비자보호처럼 평소엔 몰라도 되지만 막상 필요할 때 어디서 찾는지 모르는 정보가 필요하다.
- **중장년층:** 건강상담, 정부 지원 서비스를 이용하고 싶지만 복잡한 행정 용어에 진입장벽을 느낀다. 쉬운 말과 명확한 이용 방법 설명이 중요하다.
- **공통:** "급하게 검색했는데 안 나오는" 정보를 찾는 모든 사람 — 속도와 신뢰가 동시에 필요하다.

## 14. States

| State | 처리 |
|---|---|
| **빈 상태(검색 결과 없음)** | 왜 결과가 없는지 한 줄 설명(`조건에 맞는 서비스가 없어요`) + 필터 초기화 제안. 일러스트 없이, `데이터가 없습니다` 금지. |
| **빈 상태(카테고리 준비 중)** | 따뜻한 한 줄(`이 카테고리는 곧 채워질 예정이에요`) + 서비스 제보 CTA. |
| **로딩(첫 진입)** | 최종 카드 레이아웃과 동일한 크기의 `gray-200` 스켈레톤 블록. Shimmer 1.2s. |
| **로딩(무한 스크롤)** | 목록 하단에 Primary 24px 스피너. 오버레이 없음, 기존 카드는 계속 보임. |
| **오류(입력 필드)** | 테두리 `#fa342c` 2px, 헬퍼 텍스트 red-700 13px, 실행 가능한 한 문장(`다시 입력해 주세요`). |
| **오류(토스트)** | `#1a1c20` 배경, 흰색 14px 텍스트, 3초 자동 닫힘. 한 문장, 아이콘 없음. |
| **오류(네트워크/서버)** | 전체 화면 중앙 메시지 + 재시도 버튼(Primary). 일러스트 없음. |
| **성공(제보 접수)** | 전용 확인 화면(토스트 아님) — 초록 체크 아이콘, 과거형 한 문장(`제보가 접수되었어요`), 단일 확인 버튼. |
| **Disabled** | 배경 `gray-200`, 텍스트 `gray-500`. 색 반전 없음, geometry는 그대로 유지. |

## 15. Motion & Easing

**Durations:**

| 토큰 | 값 | 용도 |
|---|---|---|
| `motion-instant` | 0ms | 토글, 체크박스 상태 변화 |
| `motion-fast` | 150ms | hover, focus, 버튼 프레스, 인라인 성공 플래시 |
| `motion-standard` | 250ms | 기본값 — 카드 탭, 탭 전환, 바텀시트 |
| `motion-slow` | 350ms | 강조 전환 — 풀시트, 성공 화면 |
| `motion-page` | 300ms | 페이지 이동 |

**Easings:**

| 토큰 | 커브 | 용도 |
|---|---|---|
| `ease-enter` | `cubic-bezier(0.0, 0.0, 0.2, 1)` | 시트/토스트/페이지 진입 |
| `ease-exit` | `cubic-bezier(0.4, 0.0, 1, 1)` | 닫힘, 팝, 토스트 자동 종료 |
| `ease-standard` | `cubic-bezier(0.4, 0.0, 0.2, 1)` | 양방향 전환 — 확장 카드, 탭 콘텐츠 |

**Spring 지양.** 바운스/오버슈트 이징은 쓰지 않는다. 응급·법률·행정처럼 무거운 정보를 다루는 서비스에서 통통 튀는 모션은 가벼워 보이고 신뢰를 깎는다. `ease-enter`, `ease-exit`, `ease-standard`만 사용한다.

**시그니처 모션.**

1. **카드 탭.** 프레스 시 98% 스케일로 압축(`motion-fast / ease-standard`), 탭업 후 이동. 피드백은 즉시, 페이지 전환은 `motion-page / ease-enter`로 뒤따른다.
2. **바텀시트 등장.** `y+40px`에서 `motion-standard / ease-enter`로 올라오며 배경 페이드가 동시에 진행. 닫힘은 `motion-fast / ease-exit` — 나가는 쪽이 더 가볍다.
3. **카테고리 전환.** 슬라이드가 아니라 크로스페이드(`motion-slow`) — 카테고리 간에는 방향성이 없으므로 슬라이드는 오해를 준다.
4. **Reduce motion.** `prefers-reduced-motion: reduce`에서는 모든 `motion-*` 토큰이 `motion-instant`로 수렴. 크로스페이드가 슬라이드를 대체.

<!--
OmD v0.1 — bootstrapped from `karrot` reference (inspired mode, no axis deltas applied).
§1-9 토큰(컬러/타이포/스페이싱/라운드)은 karrot 원본 값을 그대로 계승 — 사용자 요청에 명시적 축 변경 없었음.
§10-15는 PRD.md(핵심 가치제안·타겟·미션) 및 사용자 확인 답변(도메인 yeogida-life.com, 이미 오픈 상태) 기반으로 재작성.
§13 Personas는 PRD.md §2 실제 타겟 세그먼트를 그대로 사용 — 가상 인구통계 아님.
-->
